
upstream ui {
  server ui:80;
}

upstream api {
  server api:3001;
}

server {
  listen 80;

  location /api {
      rewrite /api/(.*) /$1 break;
      proxy_pass http://api;
  }

  location / {
      proxy_pass http://ui;
  }

}
